# Core
pandas>=2.0
numpy>=1.24
requests>=2.28

# Spatial
geopandas>=1.0
rasterio>=1.3
rasterstats>=0.19
shapely>=2.0
pyarrow>=14.0            # Google Open Buildings ships as GeoParquet
mapclassify>=2.5          # classification schemes for choropleths
libpysal>=4.9             # Queen contiguity weights for Moran's I / LISA
esda>=2.5                 # Moran's I and local indicators (LISA cluster maps)

# Survey microdata
pyreadstat>=1.2           # .dta/.sav with value labels intact

# Output
matplotlib>=3.7
jinja2>=3.0               # DataFrame.to_latex
scipy>=1.10
statsmodels>=0.14

# Earth Engine — TMF, Dynamic World, RADD (see gee_extract.py)
earthengine-api>=1.0
openpyxl>=3.1            # IBES published tables (.xlsx)
xlrd>=2.0              # NISR/Census-PHC: the 2012 ISCO coding list is a legacy .xls (pandas.read_excel needs xlrd)
